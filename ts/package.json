{
  "name": "agent-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A zero-trust runtime for AI agents: deny-by-default policy engine and an Ed25519-signed, hash-chained audit log (TypeScript port).",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
